---
description: KMP shared 模块结构与 expect/actual 约定
globs: shared/**/*.kt
alwaysApply: false
---

# KMP Shared 模块约定

## 模块职责

- `shared/` 只放跨平台业务逻辑（Repository、ViewModel、模型、桥接层），**不放 UI**
- `androidApp/` 使用 **Compose** 编写 Android UI
- `iosApp/` 使用 **SwiftUI** 编写 iOS UI，通过 `LoginBridge` 等调用 shared 逻辑
- 包名统一为 `com.zhaojun.kmpfirst`

## Source Set 划分

| Source Set | 用途 |
|---|---|
| `commonMain` | 共享逻辑、`expect` 声明 |
| `androidMain` | Android `actual` 实现、仅 Android API |
| `iosMain` | iOS `actual` 实现、仅 Apple API |
| `commonTest` | 不依赖平台的单元测试 |
| `androidUnitTest` / `iosTest` | 平台相关测试 |

```kotlin
// ❌ 在 commonMain 中直接 import android.* 或 platform.UIKit.*
import android.os.Build

// ✅ 在 commonMain 声明 expect，平台实现放对应 source set
expect fun getPlatform(): Platform
```

## expect/actual 规范

- `expect` 与 `actual` 保持相同包名和签名
- 平台文件命名：`{Name}.android.kt`、`{Name}.ios.kt`
- 能用 `commonMain` 纯 Kotlin 实现的，不要拆成 expect/actual

## 依赖与测试

- 依赖声明在 `shared/build.gradle.kts` 的 `sourceSets` 块
- **禁止**在 `shared` 引入 Compose、SwiftUI 等 UI 框架依赖
- 共享逻辑测试写 `commonTest`
